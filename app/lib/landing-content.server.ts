/**
 * Il testo della landing pubblica, modificabile dagli admin.
 *
 * `LandingContent` è una riga sola, come `TutorialVideo` — stesso schema di
 * id fisso: non c'è un secondo testo da distinguere.
 */

export const LANDING_CONTENT_ID = "singleton";
